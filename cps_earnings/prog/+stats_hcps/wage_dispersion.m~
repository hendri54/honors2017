function wage_dispersion(setName)
% Compute and show wage dispersion by year

cS = const_hcps(setName);

for year1 = cS.yearV(:)'
   tbM = var_load_hcps('data_main', setName, year1);
   
   idxV = find(tbM.wage > 0  &  tbM.
end

end